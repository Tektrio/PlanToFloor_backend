// Este arquivo serve como ponto de entrada para o backend
// Ele simplesmente importa e executa o servidor configurado em src/server.js

const server = require('./src/server');

// O servidor já está ouvindo na porta definida em src/server.js